/* const joi = require('joi');

const productsSchema = joi.object({
  id: joi.number().required().messages({
    'any.required': '404|Product not found',
  }),
});

module.exports = productsSchema; */